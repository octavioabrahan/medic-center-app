# Variables de entorno para la base de datos
DB_USER=usuario_db
DB_HOST=localhost
DB_NAME=centro_medico
DB_PASSWORD=contraseña_segura
DB_PORT=5432

# Variables para JSON Web Token
JWT_SECRET=clave_secreta_muy_segura
JWT_EXPIRY=24h

# Variables para el servidor
NODE_ENV=development
PORT=3001

# Variables para envío de correos
MAIL_HOST=smtp.ejemplo.com
MAIL_PORT=587
MAIL_USER=usuario@ejemplo.com
MAIL_PASSWORD=contraseña_mail
MAIL_FROM=noreply@centromedico.com

# Otras variables de configuración
CORS_ORIGIN=http://localhost:3000

# Clave de encriptación para datos sensibles (32 bytes en hex)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=reemplazar_con_clave_hexadecimal_generada_aleatoriamente